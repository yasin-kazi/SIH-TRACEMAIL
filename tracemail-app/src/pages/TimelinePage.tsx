import { useParams } from 'react-router-dom'; import { getTimeline } from '../api/cases'; import { useApiResource } from '../api/useApiResource';
export default function TimelinePage(){const {caseId=''}=useParams();const r=useApiResource(()=>getTimeline(caseId),caseId);if(r.loading)return <p>Loading timeline…</p>;if(r.error)return <p className="text-error">{r.error}</p>;return <div><h1 className="font-headline-lg">Investigation Timeline</h1>{r.data?.length?r.data.map(e=><article key={e.id} className="p-3 my-2 bg-surface-container-low rounded-xl"><strong>{e.time}: {e.title}</strong><p>{e.description}</p><small>{e.type}</small></article>):<p>No timeline events are available.</p>}</div>}
